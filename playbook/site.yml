---
- name: Print os facts and some_fact
  hosts: all
  tasks:
    - name: Print OS distribution
      debug:
        msg: "Host: {{ inventory_hostname }}, OS: {{ ansible_distribution }}"
    
    - name: Print some_fact value
      debug:
        msg: "some_fact for {{ inventory_hostname }} = {{ some_fact }}"
